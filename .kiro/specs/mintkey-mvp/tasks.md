# Mintkey MVP — Implementation Tasks

## Task Dependency Graph

```mermaid
graph LR
  M1.0[M1.0 Foundation] --> M1.1[M1.1 Login]
  M1.1 --> M1.2[M1.2 Services]
  M1.2 --> M1.3[M1.3 Credentials]
  M1.3 --> M1.4[M1.4 Agents]
  M1.4 --> M1.5[M1.5 MCP + Token]
  M1.5 --> M1.6[M1.6 Brokered Call]
  M1.6 --> M1.7[M1.7 Audit Viewer]
  M1.7 --> M1.8[M1.8 Rotation]
  M1.8 --> M1.9[M1.9 Revocation]
  M1.9 --> M1.10[M1.10 Dashboards]
  M1.10 --> M1.11[M1.11 CI Smoke]
  M1.11 --> M1.12[M1.12 Multi-tenant]
  M1.12 --> M1.13[M1.13 Settings + Chain]
```

## Tasks

- [x] 1. Milestone 1.0 — Foundation Skeleton
  - [x] 1.1 Liquibase changelogs — initial schema
    - Write Liquibase YAML changelogs covering all domain tables with `tenant_id UUID NOT NULL`, RLS policies (with PlatformAdmin escape OR clause), DB roles, and indexes
    - Write `tests/acceptance/test_rls_coverage.py` asserting (a) every tenant-scoped table has RLS, (b) no `qual='true'` no-op policies, (c) policy references both `current_setting('app.current_tenant', true)` AND `current_setting('app.platform_admin_view', true) = 'on'`
    - Apply RLS to tenant-scoped tables: `tenants`, `operators`, `operator_tenant_memberships`, `sessions`, `services`, `credentials`, `agents`, `permission_grants`, `audit_events`, `tenant_settings`
    - Create platform-scoped tables (no RLS, on allowlist): `admin_request_jti`, `service_identities`, `audit_chain_state`
    - Create three roles: `mintkey_migrate` (DDL, bypasses RLS), `mintkey_app` (DML; INSERT+SELECT on audit_events only), `mintkey_subscriber` (LISTEN + read-only)
    - `audit_events` carries `prev_hash BYTEA` and `hash BYTEA` columns for mandatory hash chain (ADR-0014.7)
    - _Requirements: Req 1 AC3, AC4, AC11; ADR-0008; ADR-0014.8; ADR-0015; ADR-0016.3_
  - [x] 1.2 Seed job
    - Implement `seed-job/main.py` performing 12 steps in order, idempotently (INSERT … ON CONFLICT DO NOTHING)
    - Steps: (1) wait for Postgres, (2) verify Liquibase applied, (3) INSERT default tenant `t_default`, (4) INSERT `audit_chain_state` genesis hash, (5) generate bootstrap admin + Argon2id hash + OperatorTenantMembership, (6) generate 4 service-identity boot secrets + Argon2id hash into `service_identities`, (7) generate AdminJS Ed25519 keypair (public to Vault Adapter), (8) generate Broker Ed25519 keypair (private to Vault Adapter), (9) import Keycloak realm, (10) write bootstrap secrets to `./data/bootstrap-secrets/`, (11) emit `tenant.bootstrap_completed` audit event, (12) exit 0
    - Write `tests/unit/seed/test_seed_idempotent.py` — run seed twice; assert identical state; run with `--rotate-bootstrap`; assert new secrets
    - _Requirements: Req 1 AC5, AC6, AC12; ADR-0014.2, ADR-0014.6, ADR-0014.7, ADR-0017.5_
  - [x] 1.3 Admin API skeleton — health + ready endpoints
    - Scaffold FastAPI app with `/v1/health` (200 always) and `/v1/ready` (503 until DB + Liquibase + Vault Adapter + change-channel confirmed)
    - Implement OTel middleware, tenant-context middleware (using bound parameters via `set_config()`), CSRF middleware (double-submit cookie with `X-Mintkey-Csrf`)
    - Write `tests/unit/admin_api/test_health.py`
    - _Requirements: Req 1 AC7, AC8; ADR-0008; ADR-0016.3_
  - [x] 1.4 Vault Adapter skeleton — startup + KEK loading
    - Implement Go service skeleton with KEK loading from `MINTKEY_VAULT_KEK_FILE` and gRPC server stub
    - Reject env-var fallback in production mode; keyfile on separate volume mount
    - Write `services/vault-adapter/internal/kek/loader_test.go`
    - _Requirements: Req 4 AC3; ADR-0003_
  - [x] 1.5 Credential Broker skeleton — startup + JWKS endpoint
    - Implement Go service with Ed25519 key loading from Vault Adapter and `GET /.well-known/jwks.json`
    - JWKS publishes active + retiring keys during overlap window; `kid` in JWS protected header
    - Write `services/broker/internal/keys/jwks_test.go`
    - _Requirements: Req 6 AC9; ADR-0006; ADR-0016.2; ADR-0017.8_
  - [x] 1.6 Kong-syncer skeleton — startup + health
    - Scaffold Go service with health endpoint and Postgres LISTEN/NOTIFY subscriber stub
    - Subscriber must configure tenant scope via `WithTenantScope(...)` — panics if omitted
    - Write `services/kong-syncer/internal/health/handler_test.go`
    - _Requirements: Req MT-4; ADR-0014.1_
  - [x] 1.7 Proxy Plugin skeleton — startup + go-pdk registration
    - Scaffold Go service with go-pdk registration and JWT verifier stub
    - Plugin holds NO credential cache (ADR-0014.4); has JWKS cache, revoked-agent set, jti revocation set
    - Write `services/proxy-plugin/internal/jwt/verifier_test.go`
    - _Requirements: ADR-0004; ADR-0014.4_
  - [x] 1.8 Shared Go packages — changes, ulid, otelinit, audit, svcid
    - Implement `internal/changes/subscriber.go` (reconnect + heartbeat + tenant scope enforcement)
    - Implement `internal/ulid/ulid.go` (prefixed ULIDs for all entity types)
    - Implement `internal/audit/emit.go` (single audit chokepoint with hash chain + advisory lock)
    - Implement `internal/svcid/client.go` (service identity token loader with file watch)
    - Implement `internal/otelinit/init.go` (with SDK-level redaction filter)
    - Write tests for each package
    - _Requirements: ADR-0010; ADR-0014.1; ADR-0014.2; ADR-0014.7; ADR-0017.6_
  - [x] 1.9 mintkey-models shared Python package
    - Implement Pydantic v2 schemas, SQLAlchemy 2.x async Mapped types, audit emission helper, change-channel client, service-identity client, tenant-context helper
    - Write `mintkey-models/tests/test_models.py`, `test_audit.py`, `test_tenant_ctx.py`
    - _Requirements: ADR-0012; ADR-0015_
  - [x] 1.10 docker-compose.yml — full stack definition
    - Write complete `docker-compose.yml` with 15 long-running containers + 2 one-shot jobs
    - Separate volume mounts: `vault_data`, `vault_kek` (defense in depth), `bootstrap_secrets`
    - Write `tests/acceptance/test_compose_starts.sh` — all containers healthy within 120s
    - _Requirements: Req 1 AC1; ADR-0003_
  - [x] 1.11 RLS architecture test
    - Write `tests/acceptance/test_rls_coverage.py` asserting 7 conditions: RLS on all tenant-scoped tables, no no-op policies, correct qual references, mintkey_app no BYPASSRLS, no UPDATE/DELETE on audit_events
    - _Requirements: Req 1 AC11; ADR-0008; ADR-0014.8; ADR-0016.3_
  - [x] 1.12 Service identity client library
    - Implement Go (`internal/svcid/client.go`) and Python (`mintkey_models/svcid.py`) clients
    - Token loaded from `/run/secrets/mintkey_service_token`; presented as `X-Mintkey-Service-Token` gRPC metadata; reloads on file change
    - _Requirements: Req SEC-8; ADR-0014.2_
  - [x] 1.13 AdminUiSignedRequest middleware + jti denylist
    - Implement FastAPI middleware validating Ed25519-signed JWTs from AdminJS on state-changing endpoints
    - Replay protection via `admin_request_jti` table (UNIQUE constraint; conflict = replay)
    - Public key fetched from Vault Adapter; cached 1 hour; force-refresh on verify failure
    - Write `tests/unit/admin_api/test_signed_request.py` (6 test cases)
    - _Requirements: Req SEC-5; Req 2 AC10; ADR-0014.6; ADR-0016.1_
  - [x] 1.14 SDK-level OTel redaction filter
    - Implement custom SpanProcessor in Go and Python dropping forbidden span attributes (`*_token`, `*_secret`, `*_password`, `*_passphrase`, credential-signature regex)
    - Write tests in both languages
    - _Requirements: Req OBS-2; ADR-0017.6_
  - [x] 1.15 SQL injection architecture test
    - Write `tests/acceptance/test_no_sql_injection.py` — AST-walks all `.py` files asserting no f-string SQL, no `.format()` SQL, no `+` concatenation in `text()` calls
    - _Requirements: Req SEC-1, SEC-2_

- [x] 2. Milestone 1.1 — Operator Login
  - [x] 2.1 Internal auth — Argon2id login with identical-body / equalized-timing
    - Implement `POST /v1/auth/internal-login` with Argon2id verification, server-side session creation, audit events
    - Response body and timing must be identical across failure modes (unknown user, wrong password, locked account)
    - Use fixed `DUMMY_HASH` for unknown-user verify path; truncate `username_attempted` to 200 chars
    - Account locks after 10 failed attempts within 5 minutes for same `username_attempted`
    - Write `tests/unit/admin_api/test_auth.py` including statistical timing test (n=100, ±10%)
    - _Requirements: Req 2 AC2, AC3, AC4, AC12; ADR-0017.5_
  - [x] 2.2 OIDC login via Keycloak
    - Implement OIDC login flow using `authlib` — redirect with PKCE, validate state/nonce/ID-token, resolve operator + tenant memberships, create session
    - Write `tests/unit/admin_api/test_oidc.py` (mock OIDC provider; tampered state; unknown sub)
    - _Requirements: Req 2 AC6_
  - [x] 2.3 Session middleware + CSRF protection
    - Implement session validation middleware and CSRF token middleware (double-submit cookie, `X-Mintkey-Csrf` header)
    - State-changing endpoints without CSRF header return 403 `csrf_required`; GET/HEAD bypass
    - Write `tests/unit/admin_api/test_csrf.py`
    - _Requirements: Req 2 AC11; Req SEC-6_
  - [x] 2.4 AdminJS login page
    - Configure AdminJS login page with "Internal auth" and "Login with Keycloak" options
    - Login flow does NOT use AdminUiSignedRequest middleware (bootstrap surface)
    - Write `admin-ui/tests/test_login.test.ts`
    - _Requirements: Req 2 AC1, AC8_

- [x] 3. Milestone 1.2 — Service Registration
  - [x] 3.1 Service CRUD — Admin API
    - Implement `POST/GET/PATCH/DELETE /v1/tenants/{tid}/services` with RBAC, tenant scoping, audit events
    - NOTIFY on global channel `mintkey:service` (not per-tenant) inside same DB transaction as INSERT and audit
    - Validate `base_url` against RFC1918/link-local/loopback/metadata IPs (reject with `forbidden_destination`)
    - Use bound parameters for `pg_notify`: `text("SELECT pg_notify(:channel, :payload)")`
    - Write `tests/unit/admin_api/test_services.py` (7 test cases)
    - _Requirements: Req 3 AC1–AC12; ADR-0007; ADR-0014.1_
  - [x] 3.2 Kong-syncer — service change handler
    - Implement LISTEN/NOTIFY subscriber and Kong YAML push logic
    - Generate Kong declarative YAML with explicit path route + virtual-host route per ADR-0007
    - Subscriber tenant scope: `[ALL_TENANTS]`; also subscribes to `mintkey:agent` for revocation
    - Write `services/kong-syncer/internal/kong/yaml_test.go` + integration test (NOTIFY → Kong push ≤ 5s)
    - _Requirements: Req 3 AC4; ADR-0004; ADR-0007; ADR-0014.1_
  - [x] 3.3 AdminJS — Services resource (writes via FastAPI)
    - Configure AdminJS Services resource — all writes route through `admin-api` with AdminUiSignedRequest JWT
    - `@adminjs/sql` adapter in read-only mode; AdminJS does NOT INSERT/UPDATE/DELETE directly
    - Implement `admin-ui/src/lib/signed-request.ts` (JWT signer using private key)
    - Write `admin-ui/tests/test_services.test.ts`
    - _Requirements: Req 3; ADR-0013; ADR-0014.5; ADR-0014.6_

- [x] 4. Milestone 1.3 — Credential Registration
  - [x] 4.1 Vault Adapter — PutCredential + GetCredential + ValidateServiceIdentity
    - Implement AES-256-GCM envelope encryption: generate DEK, encrypt plaintext, wrap DEK with KEK, store in SQLite
    - Implement `ValidateServiceIdentity` RPC for caller authentication
    - Scope enforcement: `svcid_admin_api` has full CRUD; `svcid_proxy` has only GetCredential
    - Write crypto, store, and gRPC tests
    - _Requirements: Req 4 AC1, AC2, AC8; ADR-0003; ADR-0014.2_
  - [x] 4.2 Admin API — credential endpoints
    - Implement `POST /v1/tenants/{tid}/services/{sid}/credentials` (create) and `POST .../test` (test-run)
    - Test endpoint: per-service rate limit (10/min), egress allowlist re-validation, configurable `{method, path, timeout_ms, body?}`
    - Audit events: `credential.registered` (no plaintext), `service.test_executed` (no body content)
    - Write `tests/unit/admin_api/test_credentials.py`
    - _Requirements: Req 4 AC4–AC9; ADR-0007; ADR-0014.2_
  - [x] 4.3 Red-team plaintext grep test
    - Write `tests/acceptance/test_no_plaintext.sh` — register credential with canary value, run test + brokered call, grep all logs + OTel exports for canary, assert zero matches
    - _Requirements: Req 4 AC4; S-SEC-1_
  - [x] 4.4 AdminJS — Credentials resource (writes via FastAPI)
    - Configure AdminJS Credentials resource — all writes routed to `admin-api`; plaintext never displayed
    - `value` field marked write-only; show view shows `***`
    - Write `admin-ui/tests/test_credentials.test.ts`
    - _Requirements: Req 4 AC1, AC4; ADR-0014.5_
  - [x] 4.5 Vault Adapter — encrypted-DEK cache
    - Add encrypted-DEK cache keyed by `(tenant_id, service_id, key_version)` with 5-min TTL
    - Cache invalidation on `credential.rotated` event from change channel
    - Cache stores encrypted DEK only — never plaintext DEK or credential
    - Write `services/vault-adapter/internal/cache/dek_cache_test.go`
    - _Requirements: Req 9 AC3; ADR-0014.4_

- [x] 5. Milestone 1.4 — Agent and Permission Management
  - [x] 5.1 Agent CRUD — Admin API
    - Implement agent CRUD with API key generation (`mk_agent_` + Crockford base32), Argon2id hashing, 8-byte fingerprint
    - Plaintext key returned exactly once in 201 response; never in subsequent reads or audit
    - Architecture test: grep `audit_events.payload` for `mk_agent_` — assert zero matches
    - Write `tests/unit/admin_api/test_agents.py`
    - _Requirements: Req 5 AC1–AC3, AC9; ADR-0017_
  - [x] 5.2 Permission grant/revoke with closed Constraints schema
    - Implement permission grant/revoke validating closed `Constraints` schema (rate_limit, time_window, request_path_prefix, source_ip_allowlist)
    - Unknown keys in Constraints → 422 `validation_failed` (Pydantic `additionalProperties=false`)
    - Idempotent re-grant with same constraints → 200; different constraints → 409 `permission_constraints_conflict`
    - Write `tests/unit/admin_api/test_permissions.py`
    - _Requirements: Req 5 AC4–AC7; ADR-0016.4; ADR-0014.1_
  - [x] 5.3 AdminJS — Agents and Permissions resources (writes via FastAPI)
    - Configure AdminJS resources — all writes routed to `admin-api` with AdminUiSignedRequest
    - API key shown in copy box with "shown once" warning; subsequent views show fingerprint only
    - Constraints validation client-side via Zod schema generated from OpenAPI
    - Write `admin-ui/tests/test_agents.test.ts`
    - _Requirements: Req 5 AC1, AC2; ADR-0014.5; ADR-0016.4_

- [x] 6. Milestone 1.5 — MCP Discovery and Token Issuance
  - [x] 6.1 MCP Server — agent authentication + tenant context
    - Implement Agent API Key validation with constant-time Argon2id
    - Auth middleware calls `admin-api` internal `POST /v1/internal/validate-agent-key` with `svcid_mcp`
    - Set `app.current_tenant` per session via `set_config()` (bound parameters)
    - Write `tests/unit/mcp_server/test_auth.py` (including timing test)
    - _Requirements: Req 6 AC1, AC2; ADR-0009; ADR-0014.2_
  - [x] 6.2 MCP Server — list_services, describe_service, get_openapi tools
    - Implement three discovery tools; `list_services` returns only services with at least one permission_grant for the agent
    - Cross-tenant isolation via RLS + auth context binding
    - Write `tests/unit/mcp_server/test_discovery.py`
    - _Requirements: Req 6 AC3, AC4; ADR-0008_
  - [x] 6.3 Credential Broker — token issuance with `tnt = tenant_id` and `kid` header
    - Implement JWT issuance: `tnt` = prefixed ULID (NOT slug); `kid` in JWS protected header
    - Claims: `iss`, `sub`, `aud`, `tnt`, `scope`, `jti`, `iat`, `exp`, optional `cnf.jkt`, `kid`
    - Caller must present `svcid_broker` or `svcid_mcp` service token
    - Write `services/broker/internal/issuer/issuer_test.go`
    - _Requirements: Req 6 AC6, AC8; ADR-0006; ADR-0008; ADR-0017.8, ADR-0017.11_
  - [x] 6.4 MCP Server — request_token with constraint evaluation
    - Implement `request_token` with closed Constraints evaluation (rate_limit, time_window) before delegating to Broker
    - `request_path_prefix` and `source_ip_allowlist` evaluated by proxy at request time, not MCP at issuance
    - Write `tests/unit/mcp_server/test_request_token.py`
    - _Requirements: Req 6 AC5, AC10; ADR-0016.4_
  - [x] 6.5 Token issuance performance test
    - Write `tests/acceptance/test_token_issuance_perf.py` — 100 concurrent requests/sec for 30s; assert p99 ≤ 50ms
    - _Requirements: Req 6 AC9; S-PERF-2_
  - [x] 6.6 MCP Server — change channel subscriber (global channels)
    - Implement LISTEN/NOTIFY subscriber for cache invalidation and agent revocation
    - Subscribes to `mintkey:service`, `mintkey:agent` (global channels); `WithTenantScope([ALL_TENANTS])`
    - `agent.revoked` terminates active session within ≤ 5s
    - Write `tests/unit/mcp_server/test_changes.py`
    - _Requirements: Req 10 AC2; ADR-0014.1_
  - [x] 6.7 JWKS force-refresh rate limiter
    - Rate-limit JWKS force-refresh to one per `(verifier_instance, kid)` per minute
    - Prevents JWKS hammering by malformed JWTs (DoS surface)
    - Write `services/proxy-plugin/internal/jwt/jwks_refresh_test.go`
    - _Requirements: Req SEC-10; ADR-0016.2_

- [x] 7. Milestone 1.6 — Brokered Call End-to-End
  - [x] 7.1 Proxy Plugin — JWT verification
    - Implement full JWT verification: signature, exp (±30s skew), iss, aud, tnt (against service's tenant_id), scope, jti denylist, agent revocation set
    - Unknown `kid` triggers JWKS force-refresh (rate-limited per T-6.7)
    - `tnt` checked against registered service's `tenant_id` from in-memory service config map
    - Write `services/proxy-plugin/internal/jwt/verifier_test.go` (one test per claim check)
    - _Requirements: Req 7 AC1–AC3, AC12; ADR-0006; ADR-0008_
  - [x] 7.2 Proxy Plugin — credential injection per auth_scheme
    - Implement credential injection for all 7 auth schemes: `api_key_header`, `api_key_query`, `bearer_token`, `basic_auth`, `oauth2_client_credentials`, `oidc_client_secret`, `mtls`
    - Agent's `Authorization` header always stripped before forwarding
    - mTLS: load cert+key, configure TLS dialer, zero bytes after handshake
    - Write `services/proxy-plugin/internal/credential/injector_test.go`
    - _Requirements: Req 7 AC5; ADR-0016.5_
  - [x] 7.3 Proxy Plugin — Vault Adapter gRPC client
    - Implement gRPC client with `svcid_proxy` service identity; NO plaintext cache in plugin
    - Plaintext byte slice zeroed after use (best-effort given Go GC)
    - Write `services/proxy-plugin/internal/vault/client_test.go`
    - _Requirements: Req 7 AC4; ADR-0014.2; ADR-0014.4_
  - [x] 7.4 Proxy Plugin — response scrubber
    - Strip credential echoes from response headers (`Authorization`, `Cookie`, `Set-Cookie`) and body (regex patterns)
    - Body scan bounded to first 256 KiB; emit `proxy.credential_echo_detected` audit event
    - Scrubber is idempotent: `scrub(scrub(r)) == scrub(r)`
    - Write `services/proxy-plugin/internal/scrubber/response_test.go`
    - _Requirements: Req 7 AC6, AC7; ADR-0017.6_
  - [x] 7.5 Proxy Plugin — audit emission and OTel
    - Implement `proxy.hit` audit emission via internal endpoint `POST /v1/internal/audit/emit` with `svcid_proxy`
    - OTel span `mintkey.proxy.handle_request` with allowlisted attributes only
    - Write `services/proxy-plugin/internal/audit/emitter_test.go`
    - _Requirements: Req 7 AC8; S-OBS-1_
  - [x] 7.6 Proxy Plugin — egress allowlist
    - Prevent proxy from following cross-origin redirects; disable Kong's `follow_redirects` by default
    - Same-origin redirects configurable (default allowed)
    - Write tests for redirect handling
    - _Requirements: Req 7 AC13; ADR-0007_
  - [x] 7.7 Proxy Plugin — change channel subscriber
    - Subscribe to `mintkey:agent` for `agent.revoked` and `token.revoked` events only
    - Plugin does NOT subscribe to `mintkey:credential` (Vault Adapter handles that)
    - Write integration test: NOTIFY → deny within ≤ 5s
    - _Requirements: Req 9 AC3; ADR-0014.1; ADR-0014.4_
  - [x] 7.8 End-to-end brokered call integration test
    - Write `tests/acceptance/test_brokered_call.py` — full integration with testcontainers
    - Assert: 200 from Kong, mock-backend log shows real API key (not JWT), `proxy.hit` audit in chain, OTel trace has all expected spans, red-team checks pass
    - _Requirements: Req 7; S-SEC-1; S-OBS-1_
  - [x] 7.9 Proxy latency benchmark
    - Write `tests/acceptance/test_proxy_latency.py` — 100 RPS for 30s; assert p50 ≤ 10ms, p99 ≤ 30ms added latency
    - _Requirements: Req 7 AC10; S-PERF-1_
  - [x] 7.10 Control plane availability test
    - Write `tests/acceptance/test_avail.py` — issue JWT, stop control plane containers, assert brokered requests still succeed (proxy uses cached JWKS + Vault Adapter still up)
    - _Requirements: Req 7 AC11; S-AVAIL-1_

- [x] 8. Milestone 1.7 — Audit Log Viewer
  - [x] 8.1 Audit log API endpoint
    - Implement `GET /v1/tenants/{tid}/audit` with cursor pagination and filters (agent_id, service_id, event_type, time range)
    - Response time ≤ 2s for 1-hour window in tenant with ≤ 1M events
    - Write `tests/unit/admin_api/test_audit.py`
    - _Requirements: Req 8 AC1–AC6_
  - [x] 8.2 Audit table append-only enforcement test
    - Write `tests/acceptance/test_audit_append_only.py` — connect as `mintkey_app`; assert UPDATE/DELETE denied on `audit_events`; INSERT/SELECT succeed
    - _Requirements: Req 8 AC7; Req AUD-2_
  - [x] 8.3 Audit chokepoint architecture test
    - Write `tests/acceptance/test_audit_coverage.py` — AST-walk all FastAPI route handlers; assert every DB-write handler also calls `audit_emit()`
    - _Requirements: Req 8; Req AUD-1_
  - [x] 8.4 AdminJS — Audit Log resource
    - Configure AdminJS Audit Log resource with read-only list, filters, and pagination
    - Write `admin-ui/tests/test_audit.test.ts`
    - _Requirements: Req 8 AC1–AC5_
  - [x] 8.5 Audit hash chain + per-tenant advisory lock
    - Implement mandatory audit hash chain in `audit_emit()` (Go and Python)
    - Genesis: `sha256("mintkey-audit-genesis-v1:" || tenant_id)`; hash: `sha256(canonical_json(event_minus_hash) || prev_hash)`
    - Per-tenant advisory lock for ordering; `audit_chain_state` table tracks head
    - Write tests including PBT (insert N random events, recompute chain, assert all hashes match)
    - _Requirements: Req 8 AC8; ADR-0014.7_

- [x] 9. Milestone 1.8 — Credential Rotation
  - [x] 9.1 Vault Adapter — RotateCredential
    - Implement `RotateCredential` gRPC method: stores new value with `key_version + 1`; old versions remain readable
    - Atomic swap — no window where neither version is available
    - Write `services/vault-adapter/internal/store/sqlite_test.go` (extend)
    - _Requirements: Req 9 AC1, AC6_
  - [x] 9.2 Admin API — credential rotation endpoint
    - Implement rotation (same endpoint as create; detects `key_version > 1`)
    - Audit event `credential.rotated` with `previous_key_version` + `key_version`; NOTIFY on `mintkey:credential` in same transaction
    - Write `tests/unit/admin_api/test_rotation.py`
    - _Requirements: Req 9 AC2, AC6; ADR-0014.1_
  - [x] 9.3 Vault Adapter — credential cache invalidation on rotation
    - Write `tests/acceptance/test_rotation_propagation.py` — rotate credential; measure time until all calls use new version; assert ≤ 30s with zero failures
    - _Requirements: Req 9 AC3–AC5; ADR-0014.4_
  - [x] 9.4 AdminJS — Credential rotation action
    - Add "Rotate" action to Credentials resource; POSTs to `admin-api` with AdminUiSignedRequest
    - Write `admin-ui/tests/test_rotation.test.ts`
    - _Requirements: Req 9; ADR-0014.5_

- [x] 10. Milestone 1.9 — Agent Revocation
  - [x] 10.1 Admin API — agent revocation endpoint
    - Implement `POST /v1/tenants/{tid}/agents/{aid}/revoke` — set status='revoked', emit audit, NOTIFY on global `mintkey:agent` in same transaction
    - Write `tests/unit/admin_api/test_revocation.py`
    - _Requirements: Req 10 AC1; ADR-0014.1_
  - [x] 10.2 Revocation propagation end-to-end test
    - Write `tests/acceptance/test_revocation_timing.py` — create agent, issue JWT, start calls, revoke; assert denied within ≤ 5s; revoked agent gets 401 at MCP and proxy
    - _Requirements: Req 10 AC5; S-OPS-1_
  - [x] 10.3 Change channel reconciliation on reconnect
    - Write `tests/acceptance/test_reconciliation.py` — disconnect subscriber, fire events, reconnect; assert catch-up via `GET /v1/changes?since=<event_id>`
    - Handle `since=<unknown>` → 410 Gone with `oldest_known_event_id` (ADR-0017.7)
    - Implement `admin-api/src/admin_api/api/changes.py`
    - _Requirements: Req 10 AC7; ADR-0010; ADR-0017.7_
  - [x] 10.4 AdminJS — Agent revocation action
    - Add "Revoke" action to Agents resource; POSTs to `admin-api` with AdminUiSignedRequest
    - Write `admin-ui/tests/test_revocation.test.ts`
    - _Requirements: Req 10; ADR-0014.5_

- [x] 11. Milestone 1.10 — Observability Dashboards
  - [x] 11.1 OTel Collector redaction (defense in depth)
    - Configure OTel Collector with attribute deletion + `redaction` processor as second layer
    - Write `tests/acceptance/test_otel_collector_redaction.py`
    - _Requirements: Req 11 AC6; ADR-0017.6_
  - [x] 11.2 Prometheus metrics — all containers
    - Ensure all containers emit required metrics: RED metrics, `mintkey_token_issued_total`, `mintkey_proxy_hit_total`, `mintkey_vault_dek_cache_hit_total`, `mintkey_changes_subscriber_lag_seconds`
    - Write `tests/acceptance/test_metrics.py`
    - _Requirements: Req 11 AC2–AC4_
  - [x] 11.3 Grafana dashboards — pre-provisioned
    - Create 4 pre-provisioned dashboards: `mintkey-overview`, `mintkey-per-service`, `mintkey-credential-cache`, `mintkey-audit`
    - Write `tests/acceptance/test_grafana.py`
    - _Requirements: Req 11 AC1–AC4_
  - [x] 11.4 End-to-end trace verification
    - Write `tests/acceptance/test_e2e_trace.py` — make brokered call, query Jaeger, assert trace contains all expected spans
    - _Requirements: Req 11; S-OBS-1_

- [x] 12. Milestone 1.11 — CI Smoke Test and Demo Script
  - [x] 12.1 Mock backend — multi-scheme HTTP service
    - Implement FastAPI mock backend with 11 endpoints exercising all 7 auth schemes + utility endpoints
    - Endpoints: `/health`, `/api-key-header`, `/api-key-query`, `/bearer`, `/basic-auth`, `/oauth-protected`, `/mtls`, `/echo`, `/timeout`, `/5xx`, `/redirect-internal`, `/redirect-external`
    - Every endpoint logs received auth header at INFO level (proves Mintkey swapped credential)
    - Write `mock-backend/tests/test_mock.py`
    - _Requirements: Req 12 AC2; ADR-0007; ADR-0016.5_
  - [x] 12.2 Full E2E CI smoke test
    - Write `tests/acceptance/test_e2e_smoke.sh` — 13 steps of E2E-01 builder happy path
    - Assert: total runtime ≤ 90s after compose healthy; Kong returns 200; mock backend log shows real API key; audit log has all 9 event types; Jaeger trace exists; red-team grep zero matches
    - _Requirements: Req 12 AC1–AC5; S-TEST-1_
  - [x] 12.3 CI pipeline configuration
    - Configure `.github/workflows/ci.yml` with linters, unit tests, integration tests, architecture tests, schema-integrity gates, smoke test
    - _Requirements: Req 12_
  - [x] 12.4 Mock backend — Mintkey service registration via seed
    - Extend seed job to register mock backend as Mintkey service + create demo agent + grant permissions (opt-in via `MINTKEY_SEED_DEMO=true`)
    - Write `tests/acceptance/test_mock_backend_registered.py`
    - _Requirements: Req 12 AC2_
  - [x] 12.5 OpenAPI parity CI gate
    - Write `tests/acceptance/test_openapi_parity.py` — start admin-api, GET /openapi.json, diff against canonical YAML, assert byte-identical
    - _Requirements: ADR-0014.3_
  - [x] 12.6 SQLAlchemy mirror diff CI gate
    - Write `tests/acceptance/test_sqlalchemy_mirror.py` — apply Liquibase, run sqlacodegen, diff against checked-in `db.py`, assert identical
    - _Requirements: ADR-0015_
  - [x] 12.7 Mermaid render CI gate
    - Write `tests/acceptance/test_mermaid_renders.sh` — find all mermaid blocks in docs, render with mmdc, assert exit 0
    - _Requirements: Req 12_

- [x] 13. Milestone 1.12 — Multi-Tenant Smoke Test
  - [x] 13.1 Tenant creation API
    - Implement `POST /v1/tenants` (PlatformAdmin only) with audit event and per-tenant `audit_chain_state` initialization
    - Non-PlatformAdmin gets 403; creation ≤ 60s; genesis hash matches
    - Write `tests/unit/admin_api/test_tenants.py`
    - _Requirements: Req 13 AC1; ADR-0017.4_
  - [x] 13.2 Cross-tenant isolation integration test
    - Write `tests/acceptance/test_tenant_isolation.py` — create two tenants, fire cross-tenant requests on every endpoint, assert zero data leakage
    - _Requirements: Req 13 AC2, AC4; S-MT-1_
  - [x] 13.3 Cross-tenant token replay test
    - Write `tests/acceptance/test_cross_tenant_token.py` — issue JWT in tenant A, use against service in tenant B, assert 401 `tenant_mismatch`
    - _Requirements: Req 13 AC3_
  - [x] 13.4 AdminJS — Tenants resource (PlatformAdmin)
    - Configure AdminJS Tenants resource visible only to PlatformAdmin with cross-tenant view escape
    - When `platform_admin_view = true`, resources skip tenant filter; FastAPI sets `app.platform_admin_view='on'`
    - Write `admin-ui/tests/test_tenants.test.ts`
    - _Requirements: Req 13 AC1, AC6; ADR-0016.3_
  - [x] 13.5 Multi-tenant CI smoke test
    - Write `tests/acceptance/test_multitenant_smoke.py` — create second tenant, register service, verify isolation, verify cross-tenant JWT rejected; all within ≤ 60s
    - _Requirements: Req 13; S-MT-1, S-MT-2_

- [x] 14. Milestone 1.13 — Admin Settings + Audit Chain Verification
  - [x] 14.1 Admin Settings endpoint
    - Implement `GET/PATCH /v1/admin/settings` (PlatformAdmin only) with closed `AdminSettings` schema
    - Unknown keys → 422; disabling `internal_auth` when `can_be_disabled=false` → 409
    - Write `tests/unit/admin_api/test_admin_settings.py`
    - _Requirements: Req 14; ADR-0016.6_
  - [x] 14.2 Audit chain verification job
    - Implement `audit-verify-job/main.py` — walk each tenant's chain, emit `audit.chain.verified` or `audit.chain.tampered`
    - Verify ≤ 1M events in ≤ 30s
    - Write `tests/unit/audit_verify/test_chain_verify.py`
    - _Requirements: Req 15; ADR-0014.7_
  - [x] 14.3 Audit chain on-demand verification endpoint
    - Implement `POST /v1/admin/audit/verify-chain?tenant_id=<tid>` (PlatformAdmin only)
    - Returns `{ok, chain_length, last_event_id, last_hash, verified_at}` or tamper details
    - Write `tests/unit/admin_api/test_audit_verify_endpoint.py`
    - _Requirements: Req 15; ADR-0014.7_
  - [x] 14.4 PlatformAdmin cross-tenant access audit
    - Emit `platform_admin.access` audit events on every PlatformAdmin cross-tenant read
    - Event emitted in target tenant's audit chain
    - Write `tests/unit/admin_api/test_platform_admin_audit.py`
    - _Requirements: Req 13 AC6; ADR-0017.4_
  - [x] 14.5 Acknowledge tamper endpoint
    - Implement `POST /v1/admin/audit/acknowledge-tamper?tenant_id=<tid>&event_id=<eid>` (PlatformAdmin only)
    - Subsequent verify-chain calls skip acknowledged event_ids; acknowledgment itself is audited
    - Write `tests/unit/admin_api/test_acknowledge_tamper.py`
    - _Requirements: Req 15_
