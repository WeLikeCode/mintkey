# Root compatibility shim — preserves `docker compose up` from repo root.
# CD-1: Docker Compose v2.20+ include: directive; detected v5.1.4 locally.
# Canonical compose file lives at infra/compose/docker-compose.yml.
include:
  - ./infra/compose/docker-compose.yml
