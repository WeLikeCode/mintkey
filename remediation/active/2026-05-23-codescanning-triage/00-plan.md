# Code-Scanning Remediation Plan -- 2026-05-23

Generated by automated triage. Source data fetched via Mintkey proxy
(`svc_01KSA6D0CZXQ9SK3HAJS7MD00M`, agent `mk_agent_1E12...P07ZEX`),
`via: kong/3.6.1`. All API calls read-only. No dismissals or PRs applied.

Endpoints: `code-scanning/alerts?state=open` (9 pages of 100, 898 total),
`dependabot/alerts?state=open` (5 total), `secret-scanning/alerts?state=open`
(0). All return HTTP 200.

## Snapshot -- total open findings

| Tool | Open | Critical | High | Moderate | Low | Note |
|---|---|---|---|---|---|---|
| CodeQL | 24 | 1 | 23 | 0 | 0 | 0 |
| Trivy | 865 | 23 | 49 | 335 | 453 | 5 |
| Scorecard | 9 | 0 | 3 | 5 | 1 | 0 |
| Dependabot | 5 | 0 | 2 | 3 | 0 | -- |
| Secret scanning | 0 | -- | -- | -- | -- | -- |

The Trivy count (865) is in the expected band for the documented "Trivy on
Debian-base images" acceptance policy (SECURITY.md L298-308 forecasts
800-900). The CodeQL+Scorecard counts are tractable; CodeQL re-fires
dominate, with one genuinely new high-confidence regression (seed-job
plaintext password stdout) flagged below.

## Tier 1 -- fix soon (NEW critical/high; actionable; not in SECURITY.md acceptance list)

| # | Tool | Rule | Severity | Location | Why fix | Proposed fix | Est effort |
|---|---|---|---|---|---|---|---|
| 1275 | CodeQL | py/clear-text-logging-sensitive-data | high | `apps/seed-job/main.py:1075` | `print(f"Bootstrap admin password: {password}")` -- bootstrap admin plaintext password emitted to stdout (-> docker logs -> aggregator). This is a genuine regression of S6 (which encrypted the on-disk write but did not remove the stdout print). Threat: any operator with `docker compose logs` access reads the bootstrap admin password. | Replace with `print("Bootstrap admin password: written to bootstrap-secrets volume (encrypted)")`. Optionally keep a one-shot first-run message that prints a fingerprint (`sha256(password)[:8]`) rather than the plaintext. | S, 20 min |
| 1276 | CodeQL | py/clear-text-logging-sensitive-data | high | `apps/seed-job/main.py:1077` | Same root cause as #1275 -- the follow-up print line (likely "tenant=... operator=..." which CodeQL is treating as tainted via the same scope). Lower confidence this one is a true positive; verify against source after #1275 is fixed -- if it survives, it is genuine; if it disappears, it was a CodeQL data-flow artifact. | Bundled with #1275 fix; re-run CodeQL after. | S, 0 min incremental |

**Why only two items in Tier 1:** Every other open CodeQL alert is either
(a) a re-fire of a site that already has the documented S1-S6/S8 sanitizer
applied (CodeQL just cannot prove the sanitizer is taint-clearing), (b)
inside a development fixture explicitly out of scope per SECURITY.md
("demo-backend, seed-job [container], mock-backend", L64 -- note: seed-job
**container** is fixture-only, but seed-job **code** is in-scope when it
handles bootstrap secrets, hence #1275 stays Tier 1), or (c) a script
under `scripts/`, `examples/` not in the C4 container view (SECURITY.md
L43-54).

## Tier 2 -- fix next sprint (moderate, actionable)

| # | Tool | Rule / CVE | Severity | Location | Why fix | Proposed fix | Est effort |
|---|---|---|---|---|---|---|---|
| dep-11 | Dependabot | CVE-2026-44432 urllib3 GHSA-mf9v-mfxr-j63j | high | `ci/lint-contracts-requirements.txt` (direct pip) | Decompression-bomb safeguard bypass in urllib3 2.6.0-2.7.0 streaming API; affects CI lint runner only (not runtime services). Fix is a clean version bump. | Bump `urllib3` constraint to `>=2.7.0`. CI-only dep, no runtime regression risk. | S, 10 min |
| dep-10 | Dependabot | CVE-2026-44431 urllib3 GHSA-qccp-gfcp-xxvc | high | `ci/lint-contracts-requirements.txt` (direct pip) | Sensitive headers forwarded across origins in low-level proxied redirects. Bundles with dep-11; same bump fixes both. | Same bump as dep-11. | S, 0 min incremental |
| dep-13 | Dependabot | CVE-2026-8723 qs GHSA-q8mj-m7cp-5q26 | medium | `apps/admin-ui/pnpm-lock.yaml` (transitive) | `qs` 6.11.1-6.15.1 DoS via `qs.stringify` TypeError; admin-ui transitive dep. AdminJS or Express dependency chain. | `pnpm update qs` in admin-ui workspace, or override in `pnpm-workspace.yaml` to `>=6.15.2`. Verify upstream consumer (AdminJS) does not pin <6.15.2. | S, 30 min |
| dep-12 | Dependabot | CVE-2026-41907 uuid GHSA-w5hq-g745-h8pq | medium | `apps/admin-ui/pnpm-lock.yaml` (transitive) | uuid v3/v5/v6 missing buffer bounds check (silent partial writes). Transitive in admin-ui; the admin-ui issues only `uuidv4()` per audit, so reachability is low. Still worth a bump. | `pnpm update uuid` to >=11.1.1 (or >=12.0.1, >=13.0.1 per range). Currently 9.0.1 per Trivy match -- the patched line for the 9.x series is `>=11.1.1`. | S, 20 min |
| dep-9 | Dependabot | CVE-2026-25645 requests GHSA-gc5v-m9x4-r6x2 | medium | `ci/lint-contracts-requirements.txt` (direct pip) | `requests.utils.extract_zipped_paths()` insecure temp-file reuse. Not called by Mintkey lint scripts directly; bundled fix with dep-10/dep-11. | Bump `requests` to `>=2.33.0` alongside urllib3 bump. | S, 0 min incremental |
| trv-* | Trivy | CVE-2026-33845 + CVE-2026-42010 libgnutls30 | critical | All 5 service images (deb12u5 or deb12u6 -> deb12u7 available) | Patched version `3.7.9-2+deb12u7` is now available upstream. Re-pin all 5 service images to the latest patched digest after the next Container Scan cron re-baseline. This is the S2-policy "wait for Debian -> repin" flow. | Owner-led repin sprint -- pull latest digest for each `python:3.12-slim-bookworm`, `node:22-bookworm-slim`, and the astral `uv:python3.12-bookworm-slim` reference. Run `docker pull <ref> && docker inspect --format='{{index .RepoDigests 0}}' <ref>` for each; update `docker-compose.yml` and any per-app Dockerfiles. | M, 1-2 hr |
| trv-31789 | Trivy | CVE-2026-31789 openssl/libssl3 3.0.18 -> 3.0.19 | critical | `mintkey-admin-api`, `mintkey-mcp-server` | Patched at `3.0.19-1~deb12u2`. SECURITY.md L312 already documents this as a "deferred upstream rebuild" pending `ghcr.io/astral-sh/uv` rebuild. Re-verify with `docker run --rm ghcr.io/astral-sh/uv:python3.12-bookworm-slim dpkg -l openssl` on each Trivy scan -- when openssl >=3.0.19, re-pin. | No code change -- monitor and repin once upstream rebuilds. | N/A until upstream |

## Tier 3 -- already-accepted residuals (verify SECURITY.md still mentions; no code change)

| # | Tool | Rule | Location | SECURITY.md anchor | Why accepted |
|---|---|---|---|---|---|
| 1093 | Scorecard | MaintainedID | (repo-level) | "Maintained (HIGH -- auto-resolving)" L144-148 | Auto-resolves 2026-07-31 (90-day repo-age threshold). UI dismissal steps in SECURITY.md L150-156. |
| 1094 | Scorecard | CodeReviewID | (repo-level) | "Code-Review (HIGH)" L130-138 | Solo-author pre-v1 project; revisit when second contributor joins or before v1.0. UI dismissal steps L136-141. |
| 1097 | Scorecard | VulnerabilitiesID | (repo-level) | "Vulnerabilities -- GO-2026-4918 (HIGH)" L186-202 | Upstream patch available; deferred dep-bump to `golang.org/x/net v0.53.0`. **DO NOT dismiss yet** -- this is the only Scorecard item with an actionable fix path; should be a Tier 2 dep-bump session (omitted from Tier 2 table above because SECURITY.md already tracks it explicitly and the architect agreed to bundle it with the next Go dep batch). |
| 1096 | Scorecard | FuzzingID | (repo-level) | "Fuzzing (MEDIUM)" L158-170 | Post-v1.0 hardening goal; not blocking pre-alpha. UI dismissal steps L164-170. |
| 1095 | Scorecard | CIIBestPracticesID | (repo-level) | "CII-Best-Practices (LOW)" L172-184 | Pre-v1.0 badge process. UI dismissal steps L178-184. |
| 1099 | Scorecard | SASTID | (repo-level) | "SAST -- coverage rate (MEDIUM)" L234-247 | ~87% commit coverage; gap is squash intermediates; deferred to production deployment. UI dismissal steps L241-247. |
| 1089 | Scorecard | PinnedDependenciesID | `tools/deps.sh:49` | "Pinned-Dependencies -- `tools/deps.sh` curl-bootstrap (MEDIUM)" L219-232 | Third-priority fallback path; blocked on astral-sh publishing signed installer hashes. UI dismissal steps L226-232. |
| 1288 | Scorecard | PinnedDependenciesID | `apps/mock-backend/Dockerfile:15` | "Pinned-Dependencies -- `pip install --no-deps .` (MEDIUM)" L204-217 | Local-path package install with no downloadable artifact to hash. UI dismissal steps L211-217. |
| 1266 | CodeQL | py/weak-sensitive-data-hashing | `packages/python/mintkey-models/mintkey_models/audit.py:85` | "Audit hash chain integrity (SHA-256 invariant)" L268-278 | ADR-0014.7-mandated SHA-256 for tamper-evident audit chain; this is a false-positive of the rule on a non-credential-hashing site. Recommend dismissal with rationale from SECURITY.md L276. |
| 1267 | CodeQL | py/weak-sensitive-data-hashing | `apps/admin-api/src/admin_api/api/internal.py:119` | "Weak-hash acceptance" L282-294 | Fingerprint of 32-byte CSPRNG agent api_key; brute-force at 2^256 infeasible. Migration plan in `weak-hash-migration.md`; revisit before v1.0 GA. Tracked as accepted residual. |
| 1268 | CodeQL | py/weak-sensitive-data-hashing | `apps/admin-api/src/admin_api/api/proxy.py:64` | "Weak-hash acceptance" L282-294 | Same as #1267, service-key fingerprint variant. Accepted residual. |
| 1269 | CodeQL | py/full-ssrf | `apps/admin-api/src/admin_api/api/services.py:572` | (NEW -- needs SECURITY.md update) | **CodeQL re-fire of S1.** The `_check_ssrf_hostname` sanitizer is still in place at L555 (test_service_transient) and L716 (test_service); the alert fires at L572 because the sanitizer return value flow is not recognized by CodeQL data-flow. Recommend dismiss with rationale referencing S1 report. **Action item for architect**: add a "SSRF guard sanitizer" subsection to SECURITY.md mirroring the audit-chain false-positive paragraph, so future re-fires have a documented anchor. |
| trv-* (zlib1g CVE-2023-45853) | Trivy | DebianSec | 5 images | "Trivy on Debian-base images -- acceptance policy" L298-308 | NO upstream Debian fix. Policy: wait for Debian. |
| trv-* (libsqlite3 CVE-2025-7458) | Trivy | DebianSec | 4 images | Same anchor | NO upstream Debian fix. Policy: wait for Debian. |
| trv-* (~860 other Debian-base CVEs) | Trivy | DebianSec | All 5 service images | Same anchor | Documented acceptance policy: 800-900 alerts at any given time; focus only on critical-severity until distroless migration lands (out of scope per S2 owner decision). |

## Tier 4 -- dismiss with comment (false-positives, unfixable, low value)

| # | Tool | Rule | Location | Dismiss reason | Comment to paste |
|---|---|---|---|---|---|
| 1272 | CodeQL | py/clear-text-logging-sensitive-data | `apps/seed-job/main.py:396` | False positive | `_ensure_secret_file` logs the file's label and size, not the secret value. The actual `write_*` of the secret happens at L408-410, which is the S6-mitigated Fernet-encrypted path. CodeQL traces taint through the function generically. |
| 1273 | CodeQL | py/clear-text-logging-sensitive-data | `apps/seed-job/main.py:399` | False positive | Same root cause as #1272 -- `print(f"Bootstrap: {_label} INVALID (size={len(existing)}) -- regenerating.")` logs label + length, never the secret. |
| 1274 | CodeQL | py/clear-text-logging-sensitive-data | `apps/seed-job/main.py:412` | False positive | Same root cause -- `print(f"Bootstrap: wrote {_label}")` logs label only. The S6 closing report should be cited. |
| 1277 | CodeQL | py/clear-text-storage-sensitive-data | `apps/seed-job/main.py:408` | False positive (S6 sanitizer) | `path.write_text(value)` at L408 receives `value` from the generic `generate()` callback. For the `_ensure_admin_password_file` call site this value is already Fernet-encrypted bytes (per S6). For other call sites the values are random tokens, not "passwords". CodeQL cannot distinguish. Cite S6 closing report and Fernet encryption rationale. |
| 1278 | CodeQL | py/clear-text-storage-sensitive-data | `apps/seed-job/main.py:410` | False positive (S6 sanitizer) | Same as #1277 -- `path.write_bytes(value)` is the binary branch of `_ensure_secret_file`; for admin-password the bytes are Fernet ciphertext. |
| 1286 | CodeQL | py/clear-text-logging-sensitive-data | `apps/seed-job/main.py:1025` | False positive | `print(f"Mirrored admin_password to host bind: {host_file}")` logs the file path, not the password value. New alert created 2026-05-23 -- likely from a recent docs/print refactor. |
| 1287 | CodeQL | py/clear-text-logging-sensitive-data | `apps/seed-job/main.py:1031` | False positive | `print(f"WARN: could not mirror admin_password to {HOST_BOOTSTRAP_SECRETS_DIR}: {exc}")` logs path + OSError exc; password is never in scope at that print. |
| 1282 | CodeQL | go/clear-text-logging | `apps/proxy-plugin/cmd/proxy-plugin/main.go:275` | False positive (S4 sanitizer) | `safeID()` redaction is in place at L272-273. The log call at L275 receives `safeURLSvcID` and `safeSvcID`, both [redacted] for non-UUID inputs. CodeQL does not recognize `safeID` as a sanitizer despite the explicit comment at L544-553. Cite S4 closing report. |
| 1283 | CodeQL | go/clear-text-logging | `apps/proxy-plugin/cmd/proxy-plugin/main.go:314` | False positive (S4 sanitizer) | Same -- L312-313 wrap `serviceID`, `tenantID` with `safeID()` before the L314 `log.Printf`. The remaining `%v err` flow is the vault Go error which contains no credential bytes by design (Inject returns sentinels only -- S4 closing report L17). |
| 1284 | CodeQL | go/clear-text-logging | `apps/proxy-plugin/cmd/proxy-plugin/main.go:314` | False positive (S4 sanitizer) | Duplicate CodeQL alert (different path-flow root) for the same log site. Dismiss alongside #1283. |
| 1285 | CodeQL | go/clear-text-logging | `apps/proxy-plugin/cmd/proxy-plugin/main.go:314` | False positive (S4 sanitizer) | Third CodeQL alert for the same line -- typical of taint-flow tools that emit per-source-sink pair. Dismiss alongside #1283. |
| 1270 | CodeQL | py/clear-text-logging-sensitive-data | `apps/mock-backend/src/mock_backend/rest/main.py:34` | False positive (S3 sanitizer) | `logger.info("api-key-header: x_api_key=%s", _redact(x_api_key))` -- the `_redact()` helper added in S3 returns at most 4 chars + ellipsis. CodeQL cannot prove the sanitizer. The mock-backend separately echoes the unredacted key in the response body (L37 `received_key`) which is by-design for E2E testing -- mock-backend is an explicit out-of-scope fixture per SECURITY.md L64. |
| 1271 | CodeQL | py/clear-text-logging-sensitive-data | `apps/mock-backend/src/mock_backend/rest/main.py:44` | False positive (S3 sanitizer) | Same as #1270 for the api-key-query endpoint. |
| 1261 | CodeQL | py/clear-text-logging-sensitive-data | `examples/python-agent-snippet/agent.py:90` | Out of scope (fixture) | `print(f"    Agent sent     : Authorization: Bearer {jwt_preview}")` -- this is a demo agent snippet in `examples/`, the `jwt_preview` value is already a truncated display string set earlier in the demo. Per SECURITY.md scope L41-54, `examples/` is not a C4 container component. |
| 5 | CodeQL | py/clear-text-logging-sensitive-data | `scripts/e2e_smoke.py:100` | Out of scope (script) | `scripts/e2e_smoke.py` is a dev tooling script; it prints what it sends to verify the test stack end-to-end. Not a runtime credential-handling component. Same scope argument as the examples/. |
| 6 | CodeQL | py/clear-text-logging-sensitive-data | `scripts/e2e_smoke.py:108` | Out of scope (script) | Same as #5. |
| 7 | CodeQL | py/clear-text-logging-sensitive-data | `scripts/e2e_smoke.py:113` | Out of scope (script) | Same as #5. |
| 8 | CodeQL | py/clear-text-logging-sensitive-data | `scripts/e2e_smoke.py:912` | Out of scope (script) | Same as #5 -- prints the Service UUIDs (which are wire IDs, not secrets) but CodeQL has tagged the surrounding scope as password-tainted. |
| 1260 | Scorecard | PinnedDependenciesID | `.github/workflows/ci.yml:110` | Verify-then-dismiss | Need to verify which step at L110 -- if it's an unpinned action ref, fix it; if it's a documented exception, add a SECURITY.md entry. **Architect open question -- see below.** |
| 1269 | CodeQL | py/full-ssrf | `apps/admin-api/src/admin_api/api/services.py:572` | False positive (S1 sanitizer) | The `_check_ssrf_hostname` sanitizer is applied at L555 (test_service_transient) and L716 (test_service) immediately before the outbound `httpx.AsyncClient.request()` call. CodeQL data-flow does not recognize the sanitizer's `raise HTTPException` as taint-clearing. Cite S1 closing report and recommend adding a SECURITY.md anchor for "SSRF host-binding sanitizer" mirroring the audit-chain false-positive paragraph. |

## Dependabot summary by package

| # | Package | Ecosystem | Current | First patched | Manifest | Severity | CVE |
|---|---|---|---|---|---|---|---|
| 9  | requests | pip | (<2.33.0) | 2.33.0 | `ci/lint-contracts-requirements.txt` (direct, runtime in CI) | medium | CVE-2026-25645 |
| 10 | urllib3  | pip | 2.6.0-2.7.0 | 2.7.0  | `ci/lint-contracts-requirements.txt` (direct, runtime in CI) | high | CVE-2026-44431 |
| 11 | urllib3  | pip | 2.6.0-2.7.0 | 2.7.0  | `ci/lint-contracts-requirements.txt` (direct, runtime in CI) | high | CVE-2026-44432 |
| 12 | uuid     | npm | <11.1.1   | 11.1.1 | `apps/admin-ui/pnpm-lock.yaml` (transitive, runtime) | medium | CVE-2026-41907 |
| 13 | qs       | npm | 6.11.1-6.15.1 | 6.15.2 | `apps/admin-ui/pnpm-lock.yaml` (transitive, runtime) | medium | CVE-2026-8723 |

All 5 are clean dep-bumps. The urllib3+requests bumps are CI-only path
(lint-contracts runner); the qs+uuid bumps are admin-ui runtime.

## Container CVEs summary (Trivy on Debian-base -- almost all here)

| Image | Total | Crit | High | Med | Low | Note |
|---|---|---|---|---|---|---|
| `library/mintkey-admin-api`     | 183 | 6 | 15 | 67 | 94 | 1 |
| `library/mintkey-mcp-server`    | 183 | 6 | 15 | 67 | 94 | 1 |
| `library/mintkey-mock-backend`  | 169 | 4 |  7 | 64 | 93 | 1 |
| `library/mintkey-seed-job`      | 169 | 4 |  7 | 64 | 93 | 1 |
| `library/mintkey-admin-ui`      | 140 | 3 |  3 | 58 | 75 | 1 |
| `library/mintkey-jaeger-auth`   |   8 | 0 |  0 |  5 |  3 | 0 |
| `bin/oauth2-proxy` (bundled bin) |  3 | 0 |  0 |  3 |  0 | 0 |
| `pip-25.0.1` METADATA           |   4 | 0 |  0 |  3 |  1 | 0 |
| npm node_modules (picomatch / ip-address / brace-expansion / uuid@9 / qs@6.15.1) | 6 | 0 | 2 | 4 | 0 | 0 |

**Critical-severity breakdown (23 alerts):**

| Package           | Versions                                       | Fixed version  | CVE              | Images affected | Action  |
|---|---|---|---|---|---|
| `libgnutls30`     | 3.7.9-2+deb12u5 (admin-api, mcp-server), 3.7.9-2+deb12u6 (mock-backend, seed-job, admin-ui) | 3.7.9-2+deb12u7 | CVE-2026-42010 | 5 | repin |
| `libgnutls30`     | same as above | 3.7.9-2+deb12u7 | CVE-2026-33845 | 5 | repin |
| `libssl3`         | 3.0.18-1~deb12u2 | 3.0.19-1~deb12u2 | CVE-2026-31789 | admin-api, mcp-server | repin (blocked: astral/uv rebuild) |
| `openssl`         | 3.0.18-1~deb12u2 | 3.0.19-1~deb12u2 | CVE-2026-31789 | admin-api, mcp-server | repin (blocked: astral/uv rebuild) |
| `zlib1g`          | 1:1.2.13.dfsg-1 | NO UPSTREAM FIX | CVE-2023-45853 | 5 | accept (SECURITY.md L298) |
| `libsqlite3-0`    | 3.40.1-2+deb12u2 | NO UPSTREAM FIX | CVE-2025-7458 | 4 (all except admin-ui) | accept (SECURITY.md L298) |

Of 23 critical Trivy alerts:
- **10 actionable now**: libgnutls30 CVE-2026-33845 (x5 images) + CVE-2026-42010 (x5 images). Fix = repin all 5 service images to digests carrying `libgnutls30 3.7.9-2+deb12u7`.
- **4 actionable but blocked upstream**: openssl + libssl3 CVE-2026-31789 (x2 packages x 2 images = 4 alerts), waiting on the `ghcr.io/astral-sh/uv:python3.12-bookworm-slim` rebuild per SECURITY.md L312.
- **9 wait-for-Debian accept**: zlib1g CVE-2023-45853 (x5 images) + libsqlite3 CVE-2025-7458 (x4 images) -- no upstream patch available. Covered by SECURITY.md L298-308 policy.

## Suggested execution order

1. **Tier 4 dismissals first** (cheapest signal cleanup, 18 alerts).
   - 6 CodeQL false-positives in seed-job/mock-backend (cite S3+S6).
   - 4 CodeQL false-positives in proxy-plugin (cite S4).
   - 4 CodeQL out-of-scope alerts in `scripts/` + `examples/` (cite SECURITY.md L41-64).
   - 1 CodeQL SSRF re-fire at services.py:572 (cite S1; **first add SECURITY.md anchor**).
   - 1 CodeQL re-fire on agent-snippet line 90.
   - 2 CodeQL re-fires (1270, 1271) in mock-backend response-echo paths.
   - **Suggested order within Tier 4**: do the SECURITY.md SSRF-sanitizer anchor first (~10 min), then bulk-dismiss with the comments above.
   - **Architect open question** (#1260, `.github/workflows/ci.yml:110`): inspect first, do not bulk-dismiss.
2. **Tier 1 actionable fixes** (2 alerts, 1 fix).
   - Single PR: fix `apps/seed-job/main.py:1075` (remove plaintext password stdout print).
   - This is the only genuinely-new finding in the queue.
3. **Tier 2 batch** (5 dependabot + 1 image repin sprint).
   - PR 1: ci/lint-contracts dep bump (requests + urllib3, single change).
   - PR 2: admin-ui dep bump (qs + uuid, may require pnpm overrides).
   - PR 3 (separate session): image repin for libgnutls30 (5 services). Trigger the Container Scan cron after merge to re-baseline Trivy SARIF.
4. **Verify Tier 3** still aligns with SECURITY.md.
   - 13 items, all anchored to existing SECURITY.md sections. No action needed unless the SECURITY.md text drifts or a new ADR supersedes ADR-0014.7.
5. **Defer**: Scorecard VulnerabilitiesID (GO-2026-4918) -- not in Tier 2 because
   SECURITY.md L186-202 already tracks it as a follow-up. Bundle with the next Go
   dep batch; recommend a new session `2026-05-DD-go-dep-bump-x-net-053` after the
   image repin lands.

## Open questions for the architect

1. **CodeQL re-fires that already have documented sanitizers** -- should SECURITY.md
   gain a new section called something like "CodeQL sanitizer false-positives"
   listing the four sanitizer patterns (`_check_ssrf_hostname`, `_redact`,
   `safeID`/`safeInjectErr`, S6 Fernet encryption) so future re-fires have a
   single anchor to cite during dismissal? At present only the audit-chain SHA-256
   case has an explicit "false positive" paragraph in SECURITY.md (L276); the other
   three sanitizers are documented in the per-session reports but not in SECURITY.md.
   Adding this would let bulk-dismissals reference one stable anchor instead of
   five different session paths. Estimated effort: 30 minutes of SECURITY.md edit.

2. **#1260 PinnedDependenciesID at `.github/workflows/ci.yml:110`** -- I did not
   open the workflow to verify whether the unpinned ref is a real omission or
   a documented exception. If it's a `setup-uv` / `setup-buildx` / similar with
   a tag rather than a SHA, it is fixable in one line; if it's documented as a
   "third-priority fallback" like `tools/deps.sh`, it belongs in SECURITY.md.
   Need owner triage before I file Tier 1 vs Tier 3.

3. **Tier 1 #1276 (`seed-job/main.py:1077`)** -- low confidence whether this is
   a genuine sibling regression or a CodeQL data-flow spill from #1275. Re-scan
   after #1275 is fixed; if it survives, escalate to genuine; if it disappears,
   was an artifact of the same taint source.

4. **Mock-backend `received_key` echo (L37/47)** -- mock-backend is explicitly
   out of scope per SECURITY.md L64, but the response body deliberately returns
   the unredacted credential value for E2E test assertion. CodeQL does NOT yet
   flag this (the alert sites are the `logger.info` calls, not the response
   bodies), but if future CodeQL rule updates start flagging the response body,
   should we add an explicit "mock-backend response echo" paragraph to SECURITY.md?
   Pre-emptive documentation only; no current alert.

5. **Tier 3 / Tier 4 disposition for the 18 actionable Tier-2 image-repin CVEs**:
   Should the image repin sprint trigger as its own remediation session
   (`2026-05-DD-libgnutls-repin`) or get bundled into a broader "container CVE
   sweep" cadence (e.g. quarterly)? Owner preference?
