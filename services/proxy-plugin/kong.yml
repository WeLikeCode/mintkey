# Kong DB-less declarative config — placeholder until kong-syncer writes full config (T-1.8.x).
# This minimal config lets Kong start in DB-less mode for smoke testing.
_format_version: "3.0"
_transform: true

services: []
routes: []
plugins: []
