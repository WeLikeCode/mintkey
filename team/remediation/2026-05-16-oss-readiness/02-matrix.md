# OSS Readiness — Audit Matrix

**Session:** 2026-05-16-oss-readiness
**Built by:** OSS-PHASE0 researcher
**Date:** 2026-05-16
**Status:** Phase 0 audit complete. Awaiting owner answers in `03-escalations.md` before chunk dispatch.

**Severity scale:** P0 = launch-blocker; P1 = ship-without-but-painful; P2 = polish.
**Status scale:** ⬜ pending / 🟦 in-flight / ✅ done / ⛔ escalated.

---

| # | Area | Finding | Evidence | Severity | Owner Decision Needed | Status | Assigned Chunk | Verification |
|---|---|---|---|---|---|---|---|---|
| F-01 | Legal | Root `LICENSE` file missing despite README claiming Apache-2.0 | `find . -maxdepth 2 -iname 'license*'` → no output; `README.md:17` states `Apache-2.0` | P0 | Yes — confirm Apache-2.0 is intended (E-1) | ✅ | OSS-1 | `find . -maxdepth 1 -name LICENSE` → `./LICENSE`; `head -3 LICENSE` → Apache License Version 2.0, January 2004; `wc -l LICENSE` → 200 |
| F-02 | Legal | `NOTICE` file absent (required by Apache-2.0 for attribution) | Same find command → no output; if Apache-2.0 confirmed a NOTICE is required | P1 | Depends on E-1 license answer | ✅ | OSS-1 | NOTICE not required: no vendored Apache-2.0 deps with attribution requirements; deps pulled at build time via requirements.txt/package.json/go.mod only; `find . -name 'vendor' -type d -not -path '*/node_modules/*'` → no output |
| F-03 | Placeholders | `<repo-url>` in README and QUICKSTART | `README.md:78` — `git clone <repo-url> mintkey`; `QUICKSTART.md:24` — `git clone <repo-url> mintkey && cd mintkey` | P0 | Yes — final GitHub URL (E-2) | ✅ | OSS-1 | `rg '<repo-url>' README.md QUICKSTART.md` → empty; replaced with `https://github.com/WeLikeCode/mintkey.git` (clone URL form in git clone context) |
| F-04 | Placeholders | `<TBD-by-architect>` security email in SECURITY.md | `SECURITY.md:21` — `Email: <TBD-by-architect>` | P0 | Yes — final security/maintainer email (E-3) | ✅ | OSS-1 | `rg '<TBD-by-architect>' SECURITY.md` → empty; replaced with `the+security@ciprianiacobescu.com` per E-3 |
| F-05 | Placeholders | `maintainers@example.invalid` in OpenAPI contact | `docs/architecture/contracts/rest/openapi.yaml:72` — `email: maintainers@example.invalid`; `url: https://example.invalid/mintkey` (line 71) | P0 | Yes — final security/maintainer email (E-3) | ✅ | OSS-1 | `rg 'example.invalid' openapi.yaml` → empty; replaced email with `the+security@ciprianiacobescu.com`, url with `https://github.com/WeLikeCode/mintkey`; `python3 -c "import yaml; yaml.safe_load(...)"` → parses |
| F-06 | Placeholders | `<repo-url>` in marketing `index.html` | `marketing/index.html:242` — `git clone <repo-url>` | P0 | Yes — final GitHub URL (E-2) | ✅ | OSS-1 | `grep 'repo-url' marketing/index.html` → empty; replaced `&lt;repo-url&gt;` with `https://github.com/WeLikeCode/mintkey.git` (clone URL form in git clone context) |
| F-07 | Contribution | `CONTRIBUTING.md` requires LLM co-author trailers; project/user rules forbid them | `CONTRIBUTING.md:168` — "Co-author attribution is required for LLM-assisted commits: add a `Co-Authored-By:` trailer."; `CONTRIBUTING.md:183` — `Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>` | P1 | No — resolve in favor of no trailers per `~/.claude/CLAUDE.md` | ✅ | OSS-2 | `rg 'Co-Authored-By' CONTRIBUTING.md` → no output |
| F-08 | CI | Mermaid render gate is non-blocking (`\|\| true`) | `ci.yml:171` — `uv run pytest ../tests/acceptance/test_mermaid_renders.py -v \|\| true` | P1 | No | ⬜ | OSS-3 | `rg '\|\| true' .github/workflows/ci.yml` → no output |
| F-09 | CI | Python linters (`ruff`, `mypy`) masked in Makefile with `\|\| true` | `Makefile:168-172` — all five Python lint commands end with `\|\| true` (ruff for admin-api, mcp-server, mintkey-models; mypy for admin-api, mintkey-models) | P1 | No | ⬜ | OSS-3 | `rg '\|\| true' Makefile` → no output |
| F-10 | Governance | No issue templates in `.github/` | `find .github -maxdepth 3 -type f` → only `workflows/ci.yml` and `workflows/playwright.yml`; no `ISSUE_TEMPLATE/` directory | P1 | No | ✅ | OSS-2 | `find .github/ISSUE_TEMPLATE -type f` exits 0 |
| F-11 | Governance | No PR template | `find . -name 'pull_request_template*'` → no output | P1 | No | ✅ | OSS-2 | `find .github -name 'pull_request_template*'` exits 0 |
| F-12 | Governance | `CODE_OF_CONDUCT.md` missing | `test -f CODE_OF_CONDUCT.md` → MISSING | P1 | No | ✅ | OSS-2 | `test -f CODE_OF_CONDUCT.md && echo OK` |
| F-13 | Governance | `SUPPORT.md` missing | `test -f SUPPORT.md` → MISSING | P1 | No | ✅ | OSS-2 | `test -f SUPPORT.md && echo OK` |
| F-14 | Governance | `GOVERNANCE.md` missing | `test -f GOVERNANCE.md` → MISSING | P2 | No | ✅ | OSS-2 | `test -f GOVERNANCE.md && echo OK` |
| F-15 | Dependency automation | No Dependabot or Renovate config | `find .github -name 'dependabot*' -o -name 'renovate*'` → no output | P1 | No | ⬜ | OSS-3 | `find .github -name 'dependabot.yml'` exits 0 |
| F-16 | Security automation | No CodeQL workflow | `ls .github/workflows/` → only `ci.yml` and `playwright.yml`; no CodeQL workflow | P1 | No | ⬜ | OSS-3 | `find .github/workflows -name '*codeql*'` exits 0 |
| F-17 | Security automation | No secret scanning workflow | Same — no `gitleaks`, `trufflehog`, or GitHub secret-scan workflow config | P1 | No | ⬜ | OSS-3 | `find .github/workflows -name '*secret*'` exits 0 |
| F-18 | Security automation | No container scan workflow (Trivy/Snyk) | Same — no container scan job in any workflow | P1 | No | ⬜ | OSS-3 | `find .github/workflows -name '*scan*' -o -name '*trivy*'` exits 0 |
| F-19 | Security automation | No SBOM/provenance generation or release workflow | Same — no release workflow, no SLSA/SBOM job | P1 | Yes — registry and dry-run vs live (E-4, E-5) | ⛔ | OSS-4 | `find .github/workflows -name '*release*' -o -name '*sbom*'` exits 0 |
| F-20 | Container | `.dockerignore` absent at repo root and all service roots | `find . -name '.dockerignore'` → only inside `node_modules/.pnpm/adminjs*/`; no project-authored file | P1 | No | ✅ | OSS-5 | `find . -name '.dockerignore' -not -path '*/node_modules/*'` → `./.dockerignore`, `./admin-ui/.dockerignore`, `./mock-backend/.dockerignore`, `./seed-job/.dockerignore`; `docker compose build` succeeds |
| F-21 | Container | All 10 Dockerfiles run as root (no `USER` directive) | Checked admin-api, mcp-server, admin-ui, broker, kong-syncer, proxy-plugin, vault-adapter, jaeger-auth, mock-backend, seed-job — zero `USER` lines in any of them | P1 | No | 🟦 | OSS-5 | Audit table in `docs/DEPLOYMENT.md` §"Container hardening status"; non-USER directive hardening deferred — needs per-service browser-tested redux |
| F-22 | Container | No `HEALTHCHECK` in any Dockerfile | `grep -l 'HEALTHCHECK' */Dockerfile services/*/Dockerfile` → no output | P1 | No | 🟦 | OSS-5 | Audit table in `docs/DEPLOYMENT.md` §"Container hardening status"; HEALTHCHECK addition deferred — needs per-service browser-tested redux |
| F-23 | Container | Base images pinned by tag only, not digest | All FROM lines use mutable tags: `python:3.12-slim`, `node:22-slim`, `golang:1.26-alpine`, `alpine:3.19` (no `@sha256:…`); distroless runtime images also undigested | P2 | Yes — digest pinning strategy (part of OSS-4 release policy) | 🟦 | OSS-5 | Audit table in `docs/DEPLOYMENT.md` §"Container hardening status"; digest pinning deferred to OSS-4 release policy session — needs per-service browser-tested redux |
| F-24 | Versioning | `admin-ui/package.json` version `1.0.0` conflicts with project pre-alpha status | `admin-ui/package.json:3` — `"version": "1.0.0"`; project README calls itself pre-alpha; OpenAPI says `0.1.0-experimental`; mintkey-models is `0.1.0` | P1 | No | ⬜ | OSS-4 | `grep '"version"' admin-ui/package.json` → pre-release tag |
| F-25 | Versioning | CHANGELOG mixes template versions (`0.1.0`, `0.2.0`) with Mintkey product versions (`1.0.0-mvp`, `1.0.1`, `1.1.0`) in a single file | `CHANGELOG.md` headers: `## [0.2.0] — 2026-05-16` (template), `## [1.1.0] — 2026-05-15` (Mintkey), `## [1.0.0-mvp] — 2026-05-11` (Mintkey) — no separation | P1 | No | ⬜ | OSS-4 | CHANGELOG clearly separates template vs product sections, or is split |
| F-26 | Python deps | All Python services use unbounded `>=` version ranges | `admin-api/requirements.txt`: `fastapi[standard]>=0.115`, `sqlalchemy[asyncio]>=2.0`, etc. (10 deps); same pattern in `mcp-server`, `mintkey-models`, `mock-backend`, `seed-job`, `tests` | P1 | No — deliberate policy needed | ⬜ | OSS-4 | Policy documented; `uv.lock` or pinned requirements present |
| F-27 | Deployment docs | `docs/architecture/05-deployment/README.md` is architecture-sketch only | File contains iteration-1 intent + Mermaid sketch + "Coming in iteration 2" list (concrete compose.yml, network topology, secret bootstrap). No working deployment path | P1 | Yes — production docs scope (E-6) | ✅ | OSS-5 | `docs/DEPLOYMENT.md` added (operator-facing); architecture sketch README left unchanged per hard rules; `wc -l docs/DEPLOYMENT.md` → ≥120 lines; pre-alpha warning at top; "NOT validated/integrated/documented" matrix present; hardening checklist and container audit table included |
| F-28 | Examples | `examples/` contains no Mintkey examples — only a template engagement-examples README | `examples/README.md` describes "reference engagements" framework; all entries are `*(pending)*`; only other file is `.DS_Store` | P1 | No | ⬜ | OSS-6 | At least one runnable Mintkey example in `examples/` |
| F-29 | Demo path | No documented 10-minute local demo that avoids external PATs | README "Run it locally" points at `make smoke` (requires full 17-container docker compose); github-quickstart requires a real GitHub PAT; no PAT-free mock-only path is documented as a first-contact demo | P1 | No | ⬜ | OSS-6 | `docs/guides/` contains a mock-only local demo guide |
| F-30 | MCP client guides | No client-specific setup guides (Claude Desktop, Claude Code, Cursor, mcp-cli) | `find docs/guides -name '*claude*' -o -name '*cursor*' -o -name '*mcp-cli*'` → no output; guides directory has only `github-quickstart.md` and `hermes-coingecko-quickstart.md` | P1 | No | ⬜ | OSS-6 | `find docs/guides -name '*claude-desktop*' -o -name '*cursor*' -o -name '*mcp-cli*'` exits 0 |
| F-31 | Marketing | `marketing/index.html` contains `<repo-url>` placeholder in "Try it" section | `marketing/index.html:242` — `git clone <repo-url>` in the quick-start code block | P0 | Yes — depends on E-2 (repo URL) | ✅ | OSS-1 | `grep 'repo-url' marketing/index.html` → empty; replaced with `https://github.com/WeLikeCode/mintkey.git` per E-2 (closed by OSS-1 as the placeholder owner) |
| F-32 | Marketing | No comparison section vs alternatives; no explicit CTA buttons | `marketing/index.html` links to docs and CONTRIBUTING but has no "Star on GitHub", "Try locally", or comparison-with-alternatives section | P2 | No | ⬜ | OSS-7 | Marketing pages have clear CTA + comparison section |
| F-33 | Marketing | Marketing links reference internal relative paths (`../docs/…`, `../CONTRIBUTING.md`) that break when served from a public URL | `marketing/index.html:25,26,137,150` etc — all doc links are `../docs/…` relative paths; won't work if hosted separately | P2 | No | ⬜ | OSS-7 | Marketing pages work standalone or links are clearly repo-relative |
